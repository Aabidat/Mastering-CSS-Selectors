# Exercise 2
## The Grouping & Nesting Debug
1. I used the child selector to target the `<a>` inside the `<nav>`, for the rule to target only the `<a>` inside the `<nav>`
2. It is trying to make the color of the `h2 and h3` blue, this rule does not work because a comma has to separate the `h2 and h3` before the group selector would be able to work.

3.